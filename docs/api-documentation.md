# API Documentation

Initial endpoints:

- `GET /health`
- `GET /complaints/`
- `GET /hotspots/`
- `GET /analytics/`
- `GET /relationships/`

Detailed request/response contracts will be added as the services are implemented.
